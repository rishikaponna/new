<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= title %></title>
    <link rel="icon" type="image/x-icon" href="/img/vote.png">
    <style>
        body{
          background-size:cover;
        }
        #round{
            border-radius: 50px;
        }
        .u{
            text-decoration: underline;
        }
        .one{
            display: flex;
            flex-direction: row;
        }
        .two{
            display: flex;
            flex-direction: row;
        }
        .six{
                background-color: rosybrown;
                border: 2px solid black;
            }
        .z{
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;

        }
      </style>
</head>
<!-- page where the voter can login using the voterid and password -->
<body class="bg-gray-100" background="img/img4.jpg">
    <nav>
        <div class="z container items-center justify-between">
          <a href="/" class="py-2 px-4 flex items-center">
            <span class="self-center text-xl font-semibold whitespace-nowrap text-cyan-800">E-Voting</span>
          </a>
        </div>
    </nav>
    <div class=" flex  text-gray-800 antialiased flex-col  overflow-hidden bg-yellow-200 py-6 sm:py-12">
        <div class=" py-3 sm:w-96 text-center">
            <span class="text-2xl font-light font-serif hover:text-indigo-900 text-black u">Login in as Voter</span>
            <div>
                <% if (messages && messages.error && messages.error.length > 0) { %>
                    <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-white my-3 list-none rounded-md px-4 py-2 font-serif"><%= messages.error[i] %></p>
                    <% } %>
                <% } %>
            </div>
            <!-- page where the voter can login using the voterid and password -->
            <div class="mt-5 shadow-md rounded-lg text-left">
                <form action="/e/<%= url %>/voter/" method="post" class="px-12 py-8">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                    <label for="voterid" class="block mt-4 font-bold font-serif text-black">Voter ID</label>
                    <input id="voterid" name="voterid" type="text" placeholder="Voter ID" class="six border w-full h-5 px-5 py-5 mt-2 rounded-md" required>
                    <label for="password" class="block mt-3 font-semibold font-serif text-black">Password</label>
                    <input id="password" name="password" type="password" placeholder="Password" class="six border w-full h-5 px-3 py-5 mt-2 hover:outline-none focus:outline-none focus:ring-indigo-500 focus:ring-1 rounded-md font-serif" required>
                    <button type="submit" id="round" class="mt-6 w-full bg-cyan-500 text-white py-2 px-6 rounded-md hover:bg-cyan-600 font-serif hover:text-indigo-900">Login</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
<!-- page where the voter can login using the voterid and password -->
<!-- page where the voter can login using the voterid and password -->